import { bg12, bg3 } from "./colors";

const theme = {
  token: {
    borderRadius: 4,
    colorPrimary: bg3,
    colorTextDescription: bg12,
    colorTextLabel: bg12,
    fontSize: 14,
  },
  components: {
    Splitter: {
      splitBarSize: 4,
    },
  },
};

export default theme;
